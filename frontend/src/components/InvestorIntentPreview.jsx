const intents=[{type:'Early-stage investor',looking:'AI × Health',stage:'Pre-seed',location:'Sydney / Australia'},{type:'University accelerator',looking:'Student founders',stage:'MVP stage',location:'Climate / AI'}]
export default function InvestorIntentPreview(){return <section className="investor-preview"><div><p>FUTURE NETWORK BRIDGE</p><h2>Investor intent</h2><span>Coming through the same intent network.</span></div><div>{intents.map((intent)=><article key={intent.type}><span>{intent.type.toUpperCase()}</span><h3>{intent.looking}</h3><p>{intent.stage} · {intent.location}</p><small>Exploratory intent signal</small></article>)}</div></section>}
