export default function FounderFilters({filters,options,onChange,onReset}){return <section className="founder-filters">{[['domain','Domain'],['stage','Stage'],['commitment','Commitment'],['capability','Capability'],['location','Location']].map(([key,label])=><label key={key}>{label}<select value={filters[key]} onChange={(event)=>onChange(key,event.target.value)}><option value="">All</option>{options[key].map((value)=><option key={value}>{value}</option>)}</select></label>)}<label>Sort<select value={filters.sort} onChange={(event)=>onChange('sort',event.target.value)}><option>Most relevant</option><option>Complementary skills</option><option>Commitment alignment</option></select></label><button type="button" onClick={onReset}>Reset</button></section>}
