import { useState } from 'react'
import { useDialogAccessibility } from '../utils/useDialogAccessibility.js'
export default function FounderInviteModal({founder,onSend,onClose}){const [message,setMessage]=useState(`Hey — I’m exploring a health-tech idea and noticed we have complementary product/ML interests. Would you be open to a quick conversation?`);const closeRef=useDialogAccessibility(onClose);return <div className="modal-backdrop" onMouseDown={(event)=>{if(event.target===event.currentTarget)onClose()}}><section className="founder-invite-modal" role="dialog" aria-modal="true" aria-labelledby="founder-invite-title"><button ref={closeRef} className="modal-close" type="button" onClick={onClose} aria-label="Close invitation">×</button><p>EXPLORATION INVITE</p><h2 id="founder-invite-title">Start a conversation with {founder.name.split(' ')[0]}</h2><label>Message<textarea rows="5" value={message} onChange={(event)=>setMessage(event.target.value)} /></label><span>{message.length} characters</span><footer><button type="button" onClick={onClose}>Cancel</button><button className="button" type="button" disabled={!message.trim()} onClick={()=>onSend(message)}>Send invitation →</button></footer></section></div>}
