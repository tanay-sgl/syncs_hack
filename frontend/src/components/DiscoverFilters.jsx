const categories = ['All','Study','Projects','Hackathons','Cofounders','Organisations','Meet']

export default function DiscoverFilters({ filters, onChange, onReset }) {
  return <section className="discover-controls"><label className="discover-search"><span aria-hidden="true">⌕</span><input value={filters.search} onChange={(event) => onChange('search', event.target.value)} placeholder="Search live intent..." /></label><div className="filter-chip-row" aria-label="Category filters">{categories.map((category) => <button className={filters.category === category ? 'active' : ''} type="button" key={category} onClick={() => onChange('category', category)}>{category}</button>)}</div><div className="discover-selects"><label>Time<select value={filters.time} onChange={(event) => onChange('time', event.target.value)}><option>Now</option><option>Today</option><option>This week</option><option>Any time</option></select></label><label>Location<select value={filters.location} onChange={(event) => onChange('location', event.target.value)}><option>Any location</option><option>Campus</option><option>Nearby</option><option>Online</option></select></label><label>Sort<select value={filters.sort} onChange={(event) => onChange('sort', event.target.value)}><option>Most active</option><option>Newest</option><option>Closest to forming</option></select></label><button type="button" onClick={onReset}>Reset</button></div></section>
}
