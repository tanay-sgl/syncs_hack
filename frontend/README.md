# Converge frontend

React and Vite frontend for Converge, a real-time human coordination platform.

```bash
npm install
npm run dev
```

Validation:

```bash
npm run lint
npm run build
```

The current hackathon demo uses frontend mock data and browser-local state. Use the subtle “Reset demo” control in the navigation to clear Converge demo data.
